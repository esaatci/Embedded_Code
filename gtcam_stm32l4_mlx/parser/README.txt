In order to reduce the number of wries to the SD card, the camera writes the MLX data in raw bytes. 

In order to convert it back to the .CSV format, this python script is necessary along with the binary. The firmware and the code has comments to explain how this works.